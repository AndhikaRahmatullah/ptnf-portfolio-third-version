import React from 'react';
//
import { NavigationBar } from './nav';

// ----------------------------------------------------------------------

const Header: React.FC = () => {
  return (
    <header>
      <NavigationBar />
    </header>
  );
};

export default Header;
